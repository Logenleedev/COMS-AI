    # base case of backtracking
    flag = True
    for key, value in X.items():
        if value == 0:
            flag = False
      
    if flag:
        return (flag, X, D)

    # select assign variable
    choose = ''
    # MRV method 
    for i in word:
        for j in number:
            coordinate = i + j
            if X[i + j] == 0:
                value = D[coordinate]
                if len(value) < MRV:
                    MRV = len(value)
                    choose = coordinate
    print(choose)